[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    slow: mark test as slow (> 10s)
filterwarnings =
    ignore::FutureWarning
    ignore::DeprecationWarning
